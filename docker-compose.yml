services:
  #  app:
  #    build: .
  #    image: skel-app
  #    container_name: "${TEAM_NAME:-local}-app"
  #    ports:
  #      - "${HOST_PORT:-8080}:8080"
  #    environment:
  #      SPRING_PROFILES_ACTIVE: prod
  #      APP_JWT_SECRET: "${APP_JWT_SECRET}"
  #    restart: unless-stopped
  redis:
    image: "redis:7.2.6"
    container_name: "${TEAM_NAME:-local}-redis"
    ports:
      - "6379:6379"
    volumes:
      - "redis_data:/data"

volumes:
  redis_data: